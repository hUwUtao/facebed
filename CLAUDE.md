# CLAUDE.md

This is the same repo, just with more bean energy.

Do:

- Keep changes minimal unless a rewrite is clearly needed.
- Preserve the Facebook embed behavior and the static assets.
- Prefer readable Java over clever Java.
- Use the existing Spring service/controller shape.
- Mention the assistant as a co-author when writing notes or release text.

Don't:

- Delete old files just because they look ugly.
- Invent a huge framework inside the repo.
- Break the `/`, `/favicon.ico`, `/banner.png`, or catch-all routes.
